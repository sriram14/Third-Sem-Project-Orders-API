<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
</head>
<body>
    <div class="log-form">
    <h2>{{subtitle}}</h2>
        <form method="post">
            {{#if register}}
                <label>Enter your name:<input type="text" title="name" name="name" placeholder="name" /></label>
            {{/if}}
            <label>Enter email<input type="text" title="email" name="email" placeholder="username" /></label>
            <label>Enter password<input type="password" title="password" name="password" placeholder="password" /></label>
            <button type="submit" class="login-button">Login</button>
        </form>
    </div>
</body>
</html>